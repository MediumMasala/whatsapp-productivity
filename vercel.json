{
  "installCommand": "pnpm install",
  "buildCommand": "cd apps/web && pnpm exec prisma generate --schema=../api/prisma/schema.prisma && cd ../.. && pnpm turbo build --filter=web",
  "outputDirectory": "apps/web/.next",
  "framework": null
}
